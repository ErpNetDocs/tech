# Send invitation mail

The **Send invitation mail** function allows administrators to send an e-mail letter and invite the newly created user to register into the instance.
The registration is partially done by now (the creation of the user into the instance), and the user needs to set his password and fill his name and other relevant information if needed, directly into the registration form.


