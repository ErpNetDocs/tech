# Shared grid behavior

The grid control comprises a collection of tools built into Client Center navigators.

It offers functionalities like sorting, filtering, and summarizing information, which are collectively responsible for displaying and organizing data in a more intuitive, use-case-specific way.

## Total summary

The **Total summary** option automatically calculates the count of documents and the total sum of their amounts across all pages of a given navigator.

The information is displayed at the bottom of the table, under the **Document No** and **Amount** columns, respectively.

<!-- Screenshot needed: cc-con-03-grid-total-summary.png -->

## Filter and search

Using the **Column filter** and the **Search** box, you can easily filter your existing documents, as well as search for and find specific ones.

### Column filter

The **Column filter** is accessible through the funnel button at the far right of a column title.

Once you expand it, you can decide exactly how that particular column should filter the navigator contents.

For instance, you may filter the **Date** column to display documents only from a specific month of a year, or the **Type name** column to show records only from a given type.

<!-- Screenshot needed: cc-con-04-grid-column-filter.png -->

### Search bar

The **Search** bar allows you to enter a word, letter, or number and then see items containing that value in any of the navigator columns.

<!-- Screenshot needed: cc-con-05-grid-search-bar.png -->

## Grouping and summarizing

You can group navigator contents by their column headers in order to organize them in a more specific tree-like hierarchical structure.

With the **Group footer summary** option, you can also see summarized information for each group, including counts for document numbers and sums for document amounts.

To group, drag and drop a column header into the **Drag a column header here to group by that column** row above the table.

You can also group by multiple columns by dragging and dropping additional column headers next to the first one.

Existing records are first grouped by the initial column, and then by each subsequent one you include.

<!-- Screenshot needed: cc-con-06-grid-grouping-and-summary.png -->

## Column chooser

The **Column Chooser** option allows you to decide which columns to display in a navigator, as well as determine their order.

To access it, click the **Column Chooser** button above the **Search** bar.

Choose which columns will be displayed by selecting the ones that you want to show.

<!-- Screenshot needed: cc-con-07-grid-column-chooser.png -->

## Save and restore layout

However you modify your Client Center navigator layout, your last preferred view is saved and restored the next time you log in, ensuring a consistent experience.

You can always reset your layout to its default view by clicking **Reset Layout**.
